# tools/

Vom Agenten generierte Werkzeuge. Jedes Tool liegt in einem eigenen
Unterordner mit Versions-Subfolders (`v0.1/`, `v0.2/` …) und einer
`spec.md`. Schreibzugriff nur über `IExtensionWorkspace`.
